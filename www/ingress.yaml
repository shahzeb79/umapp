apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: ps-ingress
spec:
  rules:
  - http:
      paths:
      - path: /
        backend:
          serviceName: ps-app-svc
          servicePort: 80
